export class Employee {
    id!: number;
    firstName!: string;
    lastName!: string;
    emailId!: string;
}

/*The non-null assertion operator(!) removes 'null' and 
'undefined' for a type without doing any explicit type checking.*/
